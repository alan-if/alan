#!/bin/sh
for f in *.output ; do
  case=`basename $f .output`
  if [ -f $case`cat .case` ] ; then
    if [ -f $case.expected ] ; then
      if [ ! -f $case.suspended ] ; then
	/bin/echo $case
      fi
    fi
  fi
done

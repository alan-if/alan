The l Isa location
    Verb v
        Does
            If o Isa object Then
                -- This should be ok because of the IF
                Say attribute Of o.
            Else
                -- Here it is not since the parameter is only restricted to thing
                Say attribute Of o.
            End If.
    End Verb.
End The l.

The o Isa object At l
End The o.

Add To Every object
  Has attribute 42.
End Add.

Syntax v = v (o) Where o Isa thing
                 Else
                     -- Here the parameter known only to *not* be an object => entity
                     Say attribute Of o.

Start At l.

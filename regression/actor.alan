options
  debug.
  pack.

location l
  exit w to l.
end location.

actor kalle name stor liten kalle at l
end actor.

syntax examine = examine (a)
  where a isa actor or object
    else "You can't do that to that."

actor hero name me
  verb examine
    does
      "$1 is here.!@#"
  end verb.
end actor.

event e1
  "Event 1."
end event.

when kalle at hero =>
  "Kalle is here."

start at l.
  schedule e1 after 2.
  "Examine Kalle & yourself."